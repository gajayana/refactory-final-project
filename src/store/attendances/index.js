import actions from '@/store/attendances/actions'
import getters from '@/store/attendances/getters'
import mutations from '@/store/attendances/mutations'
import state from '@/store/attendances/state'

export default {
  namespaced: true,
  actions,
  getters,
  mutations,
  state,
}
